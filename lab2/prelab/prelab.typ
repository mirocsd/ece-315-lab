= ECE 315 Lab 2 Prelab
== Miro Straszynski
\
1.
#image("Lab2Part1(1).png", width: 100%)
\

2.

In polling, the CPU is continously checking the device's status in a loop, meaning the task is blocking. When a task is polling, we need to use a delay between checks to avoid hogging the CPU.
